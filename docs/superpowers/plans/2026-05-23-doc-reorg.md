# Doc Reorganisation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Delete orphaned report files, clean up Notes.md, and add missing frontmatter to all specs so the docs site is tidy and consistent.

**Spec:** [2026-05-23-doc-reorg-design.md](../specs/2026-05-23-doc-reorg-design.md)

---

### Task 1: Delete Orphaned Files and Update Nav

**Files:**
- Delete: `docs/ValidationReports/` (entire directory)
- Delete: `docs/VerificationReports/` (entire directory)
- Delete: `docs/PlanningBriefs/APPROVAL_PENDING.md`
- Modify: `mkdocs.yml`

- [ ] Delete the three directories/file:
```bash
rm -rf docs/ValidationReports docs/VerificationReports
rm docs/PlanningBriefs/APPROVAL_PENDING.md
```

- [ ] Remove `APPROVAL_PENDING.md` from the nav in `mkdocs.yml` (under `Briefs & Build Logs`)

- [ ] Verify build still passes:
```bash
mkdocs build --strict
```

- [ ] Commit:
```bash
git add -A
git commit -m "docs: delete orphaned report files and stale approval note"
```

---

### Task 2: Clean Up Notes.md

**Files:**
- Modify: `docs/Notes.md`

- [ ] Rewrite `docs/Notes.md` as a structured Engineering Notes document with two sections:
  1. **KPM-1.2 Performance Gap** — Florence-2 currently 6.7s vs 2.5s target on i7-7500U. Root cause: no KV-cache (using `decoder_model` for all steps = O(n²)). Investigation path: switch to `decoder_with_past_model_int8.onnx` but blocked by fixed-16-token input dimension issue.
  2. **Naming Output Bugs** — Two open issues: (a) model outputs one of three strings ("yes" / "no" / "answering does not require reading") instead of real captions; (b) file is not renamed on disk — semantic name written to XMP only, but both the original filename and new name should be stored in XMP and the file should be renamed.

- [ ] Verify markdown renders cleanly (no escaped underscores, no raw HTML)

- [ ] Commit:
```bash
git add docs/Notes.md
git commit -m "docs: clean up Notes.md into structured engineering notes"
```

---

### Task 3: Add Missing Frontmatter to Specs

**Files:** Multiple files in `docs/superpowers/specs/`

For each spec below, add the missing fields immediately after the `# Title` line. Author is always `Alex Meyer`. Derive Scope from the file's own Goal/Summary section.

- [ ] `2026-04-17-ssh-key-update-design.md` — add `**Author**: Alex Meyer`

- [ ] `2026-04-17-stage-7-1-gui-scaffold-design.md` — add `**Author**: Alex Meyer` and `**Scope**: Tauri + Svelte GUI scaffold with six panel stubs and live status bar`

- [ ] `2026-04-19-stage-7-2-ingest-cartridge-design.md` — add full header block:
  ```
  **Date**: 2026-04-19
  **Status**: Approved
  **Author**: Alex Meyer
  **Scope**: Wire IngestDashboard and CartridgeManager panels to Python backend via PyInstaller sidecar
  ```

- [ ] `2026-04-20-cartridge-provisioning-design.md` — add full header block:
  ```
  **Date**: 2026-04-20
  **Status**: Approved
  **Author**: Alex Meyer
  **Scope**: Automated cartridge partition, format, and init via CartridgeManager panel
  ```

- [ ] `2026-04-20-ingest-pipeline-design.md` — add full header block:
  ```
  **Date**: 2026-04-20
  **Status**: Approved
  **Author**: Alex Meyer
  **Scope**: Full pipeline with per-stage checkboxes, SD auto-eject, background processing
  ```

- [ ] `2026-04-23-cartridge-identity-redesign.md` — add `**Author**: Alex Meyer`

- [ ] `2026-05-12-stage-based-cli-design.md` — add `**Author**: Alex Meyer`

- [ ] `2026-05-13-darktable-lua-plugin-design.md` — add `**Author**: Alex Meyer`

- [ ] `2026-05-14-terminal-flash-bugfix-design.md` — add `**Status**: Approved` and `**Author**: Alex Meyer`

- [ ] `2026-05-20-pipeline-v2-design.md` — add full header block:
  ```
  **Date**: 2026-05-20
  **Status**: Approved
  **Author**: Alex Meyer
  **Scope**: Replace JSONL manifest with SQLite, Darktable import at scan time, sequential file naming
  ```

- [ ] `2026-05-21-genre-aware-scoring-design.md` — add `**Author**: Alex Meyer`

- [ ] `2026-05-23-github-scaffolding-design.md` — add `**Author**: Alex Meyer`

- [ ] Commit:
```bash
git add docs/superpowers/specs/
git commit -m "docs: standardise frontmatter across all specs"
```

---

### Task 4: Update Superpowers Index and Nav

**Files:**
- Modify: `docs/superpowers/index.md`
- Modify: `mkdocs.yml`

- [ ] Add the new doc-reorg spec and plan to `docs/superpowers/index.md` tables

- [ ] Add entries to `mkdocs.yml` nav under Specs and Plans for the new doc-reorg files

- [ ] Deploy:
```bash
mkdocs gh-deploy --force
```

- [ ] Commit:
```bash
git add docs/superpowers/index.md mkdocs.yml
git commit -m "docs: add doc-reorg spec and plan to index and nav"
```
