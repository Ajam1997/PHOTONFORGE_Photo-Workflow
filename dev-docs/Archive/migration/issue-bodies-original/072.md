# #72 [KPM-1.8] Scoring Determinism
# state: OPEN
# labels: type: kpm
---
**Target:** Score variance <= 0.01 on identical image run twice (sharpness, composition, exposure, master)

**Owner:** @engineer

**Verified By:** @verification

**Last Measured:** untested

**Status:** untested

**Notes:** Run score stage twice on the same image with same model weights. Delta between run 1 and run 2 scores must be <= 0.01 for all four scalar outputs. Ensures pipeline is reproducible across runs and restarts.