**Type:** Engineering task (Closed)

## Overview

The repo needs a housekeeping pass: migrate living documents into version-controlled markdown, connect the roadmap to GitHub project management tools, and remove build artifacts and abandoned code that are tracked in git.

This breaks down into three workstreams.

---

## 1. Document Migration and Organization

### Current state
Architecture and needs documents live in various formats and locations:
- ``docs/Archive/`` — 4 ``.docx`` files (architecture v3, v4, v5, and a duplicate)
- ``docs/photonforge-architecture.md`` — current architecture (markdown, good)
- ``docs/living-user-needs.md`` — living user needs document
- ``docs/Notes.md`` — misc notes
- ``docs/superpowers/plans/`` — 11 plan files from prior development sessions
- ``docs/superpowers/specs/`` — 11 spec files from prior development sessions
- ``docs/mockups/PhotonForgePanel.jsx`` — stale JSX mockup from pre-Lua pivot
- ``CLAUDE.md`` — agent instructions (tracks architecture decisions, conventions, build sequence)
- ``.claude/agents/*.md`` — 5 agent role definitions

### Target state
- **``docs/architecture.md``** — single source-of-truth architecture document (converted from .docx, kept current)
- **``docs/user-needs.md``** — living user needs, cleaned up and current
- **``docs/roadmap.md``** — roadmap extracted from architecture doc, linked to GitHub issues/milestones (see section 2)
- **``docs/plans/``** and **``docs/specs/``** — kept as implementation history, but clearly marked as historical
- **``docs/Archive/``** — .docx files removed from tracking (available in git history if needed)
- Agent files (``CLAUDE.md``, ``.claude/agents/``) managed like code: changes go through PRs

### Actions
- [ ] Convert latest architecture .docx to markdown and merge with existing ``docs/photonforge-architecture.md``
- [ ] Review and update ``docs/living-user-needs.md`` for accuracy
- [ ] Remove stale mockup (``docs/mockups/PhotonForgePanel.jsx``)
- [ ] Clean up ``docs/Notes.md`` — integrate useful content elsewhere or remove
- [ ] Remove archived .docx files from tracking (keep in git history)

---

## 2. Link Roadmap to GitHub Systems

### Current state
The architecture document contains a build sequence / roadmap but it is not connected to any GitHub project management features. Issues exist (#9-#17) but are not organized into milestones or a project board.

### Target state
- **GitHub Milestones** for major phases (e.g. Performance & Reliability, Naming Overhaul, Experimental Features)
- **``docs/roadmap.md``** that references milestone URLs and issue numbers
- Issues tagged with milestones so progress is visible on the GitHub repo page
- Consider a GitHub Project board for kanban-style tracking

### Actions
- [ ] Create milestones for current development phases
- [ ] Assign existing issues (#9-#17) to appropriate milestones
- [ ] Extract roadmap items from architecture doc into ``docs/roadmap.md`` with issue cross-references
- [ ] Add milestone badges or links to README

---

## 3. Repo Cleanup — Remove Tracked Junk

### Build artifacts tracked in git (24 files)
These should be in ``.gitignore``, not committed:
- ``src/photo_workflow/__pycache__/*.pyc`` — compiled Python bytecode
- ``tests/__pycache__/*.pyc`` — test bytecode
- ``src/photo_workflow.egg-info/*`` — pip build metadata
- ``.superpowers/brainstorm/*/state/server.pid`` — stale PID files

### Test output photos (103 files)
- ``docs/ValidationReports/PhotoWorkFlowTestOutput/`` — 103 JPG and XMP files committed to the repo. These are full-resolution test photos that bloat the repo. Should be removed from tracking and added to ``.gitignore``. The validation report (``2026-04-18-stage7.1-validation.md``) can reference them without committing the actual images.

### Abandoned Tauri GUI (38 files)
- ``photonforge-gui/`` — entire Svelte+Tauri GUI that was abandoned when the project pivoted to the Darktable Lua plugin. 38 tracked files including Rust source, Svelte components, Cargo.lock, etc. Should be removed from tracking (preserved in git history).

### Actions
- [ ] Remove ``__pycache__/``, ``.egg-info/``, ``.pid`` files from tracking
- [ ] Remove ``docs/ValidationReports/PhotoWorkFlowTestOutput/`` from tracking
- [ ] Remove ``photonforge-gui/`` from tracking
- [ ] Update ``.gitignore`` to prevent these from being re-committed:
  ```
  __pycache__/
  *.pyc
  *.egg-info/
  *.pid
  docs/ValidationReports/PhotoWorkFlowTestOutput/
  ```

---

## Summary of removals

| Category | Files | Reason |
|---|---|---|
| Python bytecode | 18 | Build artifact, should be gitignored |
| egg-info | 6 | Build artifact |
| PID files | 2 | Stale process files |
| Test photos | 103 | Binary blobs, bloat repo |
| Abandoned GUI | 38 | Pivoted to Lua plugin |
| Stale mockup | 1 | Pre-Lua JSX file |
| Archived .docx | 4 | Available in git history |
| **Total** | **172** | **55% of tracked files** |
