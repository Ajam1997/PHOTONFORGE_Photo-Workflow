**Target:** >= 95% of outputs are non-trivial semantic captions (not 'yes', 'no', or 'answering does not require reading')

**Owner:** @engineer

**Verified By:** @verification

**Last Measured:** untested

**KPM Status:** untested

**Measurement Method:** Measured by running generate_name() over the labeled fixture corpus and counting outputs that match the known degenerate strings. Requires fixture corpus. Related to Florence-2 decode correctness (FR-1.7).

**Linked Requirements:** UN-020, FR-1.7
