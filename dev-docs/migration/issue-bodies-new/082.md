**Type:** Engineering task (Open)

## Context
Architect review of `dev-docs/architecture/scoring-module-contracts.md` flagged that the error path when `aesthetic_weighter` raises is undefined.

## Recommendation
Aesthetic failure should produce uniform weights + `fallback_used=True` flag on `FusionResult`. This avoids pipeline abort while surfacing the degraded state.

## When
Resolve during Stage 6 Step 4 (aesthetic_weighter implementation).

## Related
- `dev-docs/architecture/scoring-module-contracts.md`
- PR that introduced the contracts: feature/cartridge-manager
