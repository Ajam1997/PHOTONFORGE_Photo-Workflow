**Target:** Score variance <= 0.01 on identical image run twice (sharpness, composition, exposure, master)

**Owner:** @software_lead

**Verified By:** @verification

**Last Measured:** untested

**KPM Status:** untested

**Measurement Method:** Run score stage twice on the same image with same model weights. Delta between run 1 and run 2 scores must be <= 0.01 for all four scalar outputs. Ensures pipeline is reproducible across runs and restarts.

**Linked Requirements:** UN-012, UN-013, UN-014, FR-1.4, FR-1.5, FR-1.6
