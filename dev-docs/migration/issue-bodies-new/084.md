**Type:** Engineering task (Open)

## Context
The contracts doc under-specifies what happens on a fresh cartridge with no `aesthetic_weights` table rows.

## Recommendation
Add an explicit `aesthetic_weighter.bootstrap(weights_db)` call in the cartridge manager's init path. This seeds the SQLite table from `dev-docs/research/scoring-redesign.md §5` default priors.

## When
Resolve during Stage 6 Step 4 (aesthetic_weighter implementation).

## Related
- `dev-docs/architecture/scoring-module-contracts.md`
- `dev-docs/research/scoring-redesign.md` §5
